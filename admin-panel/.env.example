# Production build only: absolute URL of the backend API.
# In dev this is unused — vite.config.ts proxies /api and /socket.io to the backend
# (set BACKEND_URL to proxy dev to Railway instead of localhost:4000).
VITE_API_URL=https://backend-jsan-vehicletracking-production.up.railway.app
